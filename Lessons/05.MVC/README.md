#MVC

## Что такое MVC

MVC  (model-view-controller). Это способ организации кода, который предполагает выделение блоков, отвечающих за решение разных задач.


* M — Model (Модель). Model получает данные от контроллера, осуществляет выполнение нужных операций, а потом передает данные во View;

* V — View (Вид, Представление). View получает данные от Model с последующим выводом их для пользователя;

* C — Controller (Контроллер). Контроллер в MVC — это блок, обрабатывающий действия пользователя и проверяющий полученную информацию, которая затем передается в Model.

Структура:

![](../../../../Pictures/3.jpg)

## HTTP
HTTP протокол описывает правила взаимодействия между клиентом и сервером, построенное на базе запросов (Request) и ответов (Response). Каждый запрос состоит из трех частей: стартовая строка, заголовки и тело. При этом обязательной является только стартовая строка.

* Cтартовая строка:
METHOD URI HTTP/VERSION

* Заголовки — это набор пар имя-значение, разделенных двоеточием. В заголовках передается различная служебная информация: кодировка сообщения, название и версия браузера, адрес, с которого пришел клиент (Referrer) и так далее.

* Тело сообщения — это, собственно, передаваемые данные. В ответе передаваемыми данными, как правило, является html-страница, которую запросил браузер, а в запросе, например, в теле сообщения передается содержимое файлов, загружаемых на сервер. Но как правило, тело сообщения в запросе вообще отсутствует.



####Метод GET:

`GET /script.cgi HTTP/1.1\n
Host: example.com
\nn`


#### Метод POST:

`POST /script.cgi HTTP/1.1\n
Host: example.com
Content-length: 5\n
\n
Test!`

## Автолоадинг

Composer автоматически генерирует все необходимые классы для автозагрузки компонентов. Заглянув в диреткорию vendor, можно обнаружить файл autoload.php. Его включение при помощи директив require или require_once предоставляет доступ ко всем компонентам, загруженным посредством Composer

Пространство имен — это имеющий имя фрагмент программы, содержащий в себе
функции, переменные, константы и другие именованные сущности. Для того чтобы
объявить пространство имен, используется ключевое слово namespace , после которого следует имя пространства

## Суперглобальные переменные
Суперглобальные переменные — Встроенные переменные, которые всегда доступны во всех областях
$_SERVER — Информация о сервере и среде исполнения.
$_GET - Ассоциативный массив переменных, переданных скрипту через параметры URL.
Массив не только заполняется для GET-запросов, а скорее для всех запросов со строкой запроса.
$_POST - Ассоциативный массив данных, переданных скрипту через HTTP методом POST

## Дополнительная информация:

https://www.php.net/manual/ru/language.variables.superglobals.php - Суперглобальные переменные
http://www.faqs.org/rfcs/rfc3875.html - спецификация CGI
https://www.php-fig.org/psr/psr-4/ - psr4 autoloading